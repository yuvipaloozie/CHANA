# Expected manuscript outputs

For each public checkpoint, record the expected software version, manifest version, evaluation command, and checksum of the resulting summary CSV. This directory is the regression target for end-to-end reproduction.

The current scaffold does not include numeric snapshots because the underlying source-data tables and verified checkpoint registry have not yet been deposited.
