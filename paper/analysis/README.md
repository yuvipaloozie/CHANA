# Manuscript analysis

`legacy/` contains the supplied Python exports used for metrics and manuscript
figures. They retain Colab paths and assume the original private directory
structure.

Reusable numerical outputs belong in `paper/source_data/`; GraphPad Prism files
are not required when their underlying CSV data are provided.
