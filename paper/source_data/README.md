# Manuscript source data

Included now:

- `Figure_4A_pixel_PR_curves_full.csv.gz`: pixel precision-recall coordinates for all six models;
- `Table_S3_paired_statistics.csv`: paired baseline-versus-curriculum statistics; and
- `INDEX.csv`: file sizes, SHA-256 values, and manuscript links.

The final release still needs the definitive image-level/object-level results,
UMAP and feature-distance data, robustness analyses, and source tables that
reproduce the final Word figures and Tables 1-2. Add original CSV/NPZ outputs;
do not digitize values from figure graphics.
